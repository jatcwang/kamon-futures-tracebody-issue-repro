<configuration>
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.core.encoder.LayoutWrappingEncoder">
            <layout class="com.mdsol.scalalogging.logback.HumanCirceCustomDataLayout">
            </layout>
        </encoder>
    </appender>

    <!-- Uncomment the follow blocks to log JSON to a log file -->

    <appender name="FILE" class="ch.qos.logback.core.FileAppender" >
        <encoder class="ch.qos.logback.core.encoder.LayoutWrappingEncoder">
            <layout class="com.mdsol.scalalogging.logback.CirceMdsolLogLayout" >
                <component>${COMPONENT}</component>
                <version>${COMPONENT_VERSION}</version>
            </layout>
        </encoder>

        <file>${LOG_FILENAME:-log/app.log}</file>
    </appender>

    <appender name="ASYNCFILE" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="FILE"/>
        <includeCallerData>true</includeCallerData>
    </appender>

    <root level="INFO">
        <appender-ref ref="ASYNCFILE"/>
    </root>
</configuration>
